<Window x:Class="PaintOverlay.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PaintOverlay"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        WindowState="Maximized" 
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        x:Name="PaintWindow">
    <DockPanel>
        <UniformGrid DockPanel.Dock="Top" Rows="1" Columns="10" Height="50">
            <UniformGrid.Background>
                <SolidColorBrush Color="Gray" Opacity="0.9"/>
            </UniformGrid.Background>
            <TextBlock Text="Color" TextAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="Brush" TextAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="Size:" TextAlignment="Center" VerticalAlignment="Center"/>
            <TextBlock Text="Opacity:" TextAlignment="Center" VerticalAlignment="Center"/>
            <Rectangle x:Name="ColorPreview" Width="30" Height="30" Stroke="Black"/>
        </UniformGrid>
        <Canvas DockPanel.Dock="Top" x:Name="DrawingCanvas">
            <Canvas.Background>
                <SolidColorBrush Color="White" Opacity="0.01"/>
            </Canvas.Background>
        </Canvas>
    </DockPanel>
</Window>
