<Window x:Class="PaintOverlay.PreferencesWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PaintOverlay"
        mc:Ignorable="d"
        Title="PreferencesWindow" Height="450" Width="800">
    <StackPanel Margin="10,0,10,0">
        <Label x:Name="preferences_label" Content="Key Bindings" FontSize="24" />
        <Separator Background="Gray" Height="2"/>
        <Grid Margin="0,10,0,20" x:Name="preferences_grid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <TextBlock Text="Toggle Canvas Visibility" Grid.Row="0" Grid.Column="0"/>
            <TextBlock Text="Toggle Menu Visiblity" Grid.Row="1"/>

            <Button Name="canvas_visibility_bind" Grid.Column="1"/>
            <Button Name="menu_visibility_bind" Grid.Column="1" Grid.Row="1"/>

            <Button Name="canvas_visibility_reset" Content="Reset" Grid.Column="2"/>
            <Button Name="menu_visibility_reset" Content="Reset" Grid.Column="2" Grid.Row="2"/>

        </Grid>
        <Grid Grid.IsSharedSizeScope="True" HorizontalAlignment="Right" x:Name="button_grid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition SharedSizeGroup="ButtonColumn" />
                <ColumnDefinition SharedSizeGroup="ButtonColumn" />
            </Grid.ColumnDefinitions>
            <Button Content="Save Changes" Grid.Column="0" Margin="5" Click="Save_Changes" />
            <Button Content="Restore All Defaults" Grid.Column="1" Margin="5" Click="Restore_Default" />
        </Grid>
    </StackPanel>
</Window>
